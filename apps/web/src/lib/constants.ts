/**
 * App-wide constants, centralized so shared literals live in one place instead
 * of being duplicated across routes, pages, and head/meta tags.
 */

/** Human-facing site name — used in the document title, header, and OG tags. */
export const SITE_NAME = "Machined Pen Archive";
